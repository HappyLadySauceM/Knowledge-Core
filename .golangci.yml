version: "2"

run:
  timeout: 5m
  tests: true

linters:
  default: standard

formatters:
  enable:
    - gofmt
  exclusions:
    paths:
      - services/collaboration

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
